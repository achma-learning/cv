/* ============================================================================
   OPTIONAL SITE CONFIGURATION

   Most people never need to touch this file. Everything in it is served to
   every visitor, so it must never contain a token or any other credential.

   repository
       Leave it empty. The editor works out which repository is serving this CV
       by itself, and asks you to confirm if it cannot. Set it only if you host
       the CV on a custom domain and would rather not confirm by hand:

           repository: "your-handle/cv"

       Setting it changes which repository the editor *looks at*. It cannot
       grant anyone permission — GitHub decides that when the change is saved.
   ========================================================================== */
window.CV_CONFIG = {
    repository: ""
};
